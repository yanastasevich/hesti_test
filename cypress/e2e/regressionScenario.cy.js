// a stub 
describe('Perform regression testing', () => {



    before(() => {
      // Run before all tests
      cy.performLogin();
      cy.visitMap();
    });

    it('Add a polygon with 5 vertices and valid coordinates though polygon management system', () => { 
       
    });

    it('Update all coordinates with valid values through polygon management system', () => { 
    });

    it('Update polygon name', () => { 
     });

     it('Switch to marker management system', () => { 
     });

     it('Add a marker through marker management system', () => { 
     });

     it('Update the name of the marker', () => { 
     });

     it('Update the coordinates of the marker', () => { 
     });

     it('Remove the polygon through the polygon management system', () => { 
     });

     it('Remove the marker through marker management system', () => { 
     });
});
